package array.nearbyalmostduplicate;

public class NearByAlmostDuplicate {

    // k is at most diff between index
    // t is at most diff in the value
    public boolean containsNearbyAlmostDuplicate(int[] nums, int k, int t) {
        /*
        int len = nums.length;
        Set<Integer> window = new HashSet<>();
        for (int i = 0; i < len; i++) {
            if () {

            }
        }

         */
        return true;
    }
}
